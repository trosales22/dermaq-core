import Layout from "components/Layout";

const QueueReservationsPage = () => {
    return (
        <Layout>
            <h1 className="text-2xl font-bold">Queue Reservations</h1>
        </Layout>
    );
};

export default QueueReservationsPage;
